|
  history.pushState({}, null, "#{params[:page]}");
  document.getElementById('lazy_body').innerHTML = "#{ j(render partial: params[:page]) }"
  document.getElementById('lazybox').className = ''
  reset = document.querySelector('input#reset')
  if (reset) reset.focus()
